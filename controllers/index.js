const ingredients = require('./ingredientsController');
const units = require('./unitsController');
const categories = require('./categoriesController');

module.exports = {
    ingredients: ingredients,
    units: units,
    categories: categories
};